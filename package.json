{
  "name": "blood-pressure-tracker-mvp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gh-deploy": "pnpm build && gh-pages -d dist",
    "test": "vitest --run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "jsdom": "^27.1.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "@playwright/test": "^1.38.0"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "dexie": "^4.2.1"
  }
}
